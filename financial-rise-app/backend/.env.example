# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=financial_rise
DATABASE_PASSWORD=CHANGE-ME-generate-secure-password
DATABASE_NAME=financial_rise_dev
DATABASE_SSL=false

# JWT Configuration - CRITICAL SECURITY
# Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEVER commit actual secrets to version control
# Use .env.local for development (which is in .gitignore)
JWT_SECRET=CHANGE-ME-run-crypto-randomBytes-32-toString-hex
JWT_EXPIRY=15m
REFRESH_TOKEN_SECRET=CHANGE-ME-run-crypto-randomBytes-32-toString-hex
REFRESH_TOKEN_EXPIRY=7d

# Application
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# Google Cloud Storage (for local dev, use mock or local storage)
# GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
# GCS_BUCKET_NAME=financial-rise-reports-dev

# GCP Secret Manager (Production Only)
# GCP_PROJECT_ID=your-gcp-project-id

# IMPORTANT SECURITY NOTES:
# 1. Copy this file to .env.local and generate real secrets
# 2. NEVER commit .env.local to git (it's in .gitignore)
# 3. Use GCP Secret Manager in production
# 4. Secrets must be 64+ characters in production
# 5. See docs/SECRETS-MANAGEMENT.md for detailed instructions
