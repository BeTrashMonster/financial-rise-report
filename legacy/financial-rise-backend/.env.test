# Test environment variables
NODE_ENV=test
PORT=3001

# Database
DATABASE_URL=postgresql://testuser:testpass@localhost:5432/financial_rise_test

# JWT
JWT_SECRET=test-jwt-secret-key-for-testing-purposes-only-min-32-chars
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# AWS (mock values for testing)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
S3_BUCKET_NAME=test-bucket

# Email (mock values for testing)
SENDGRID_API_KEY=SG.test-api-key
FROM_EMAIL=test@example.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# CORS
CORS_ORIGIN=http://localhost:3000
